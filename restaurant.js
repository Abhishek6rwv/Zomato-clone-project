document.addEventListener("DOMContentLoaded", () => {
  const registerBtn = document.querySelector("button");

  registerBtn.addEventListener("click", () => {
    // Uncomment this line to redirect to the registration page:
    // window.location.href = "/Project 1/restaurant-registration.html";

    // Or show an alert if the form doesn't exist yet:
    alert("Registration form coming soon!");
  });
});
